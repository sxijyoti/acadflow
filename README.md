# acadflow
OOAD Mini Project

## Running the Application

Before running the application, set the database password environment variable:

```bash
export DB_PASSWORD=your_mysql_root_password
mvn spring-boot:run -DskipTests
```

The application will start on `localhost:8080`
